cmd compilation

initialization:

init

calib_step_size();
figure_out_dir();


execution:

4 cmd's
void simple_step(int num,(direction,step_duration));//trigger with a step delay，duration (in ms) 


void g_step_setup();//assume this has been called if later cmd's are called

4 cmd's
void g_step(int num,(direction,step_duration));//move $(num) step blockingly， duration (in ms) 

4 cmd's
void g_one_step((direction,min_step_duration));//move one step, blockingly;

goto_zero();
goto_pulse(int pulse_num)

goto_coordinate(double? loc)

goto_percent_coordinate(double? percent)
//goto_percent_coordinate(byte digit, byte decimal)




status:

ask location
ask step_size
ask xxx


